import { CirclePlus, Clock3 } from '../ui/icons'

export function WeeklyMenu({ menu, weekStart, weekdays, onAddMeal, getMealName, formatDayName, formatMonthShort }) {
  return <section id="menu" className="rounded-3xl border border-ink/10 bg-white p-4 shadow-soft dark:border-cream/10 dark:bg-ink-soft dark:shadow-soft-dark sm:p-6"><div className="mb-6 flex items-center justify-between"><div><h2 className="font-display text-2xl">Menú semanal</h2><p className="mt-1 text-sm text-ink/50 dark:text-cream/50">Una guía flexible para tu semana.</p></div><button onClick={() => onAddMeal({ day: 'Lunes' })} className="hidden items-center gap-2 rounded-full bg-sage px-4 py-2.5 text-xs font-bold text-moss dark:bg-moss/30 dark:text-sage sm:flex"><CirclePlus size={15} /> Agregar comida</button></div>
    <div className="space-y-3 md:hidden">{Object.entries(menu).map(([day, meals], dayIndex) => { const date = new Date(weekStart); date.setDate(date.getDate() + dayIndex); return <article key={day} className="rounded-2xl border border-ink/10 bg-cream p-4 dark:border-cream/10 dark:bg-ink"><div className="mb-3 flex items-center justify-between"><h3 className={`font-display text-xl ${dayIndex === 0 ? 'text-coral' : 'text-moss dark:text-sage'}`}>{day}</h3><div className="flex items-center gap-2"><span className="text-xs font-bold text-ink/35 dark:text-cream/35">{date.getDate()} {formatMonthShort(date)}</span><button onClick={() => onAddMeal({ day })} className="flex items-center gap-1 rounded-full bg-sage px-3 py-1.5 text-[10px] font-bold text-moss dark:bg-moss/30 dark:text-sage"><CirclePlus size={13} /> Agregar</button></div></div><div className="grid grid-cols-1 gap-2">{Object.entries(meals).map(([meal, value], mealIndex) => { const mealName = getMealName(value); return <button onClick={() => onAddMeal({ day, meal })} key={meal} className={`flex min-h-[62px] items-center justify-between gap-3 rounded-xl border border-ink/5 p-3 text-left dark:border-cream/10 ${mealIndex === 3 ? 'bg-[#fff8ef] dark:bg-ink-soft' : 'bg-white dark:bg-ink-soft'}`}><span><span className="mb-1 block text-[10px] font-bold uppercase tracking-wider text-ink/40 dark:text-cream/40">{meal}</span><span className={`block text-sm font-semibold leading-snug ${mealName ? '' : 'text-moss dark:text-sage'}`}>{mealName || '+ Agregar comida'}</span></span><span className="shrink-0 text-[10px] text-ink/40 dark:text-cream/40">{mealName ? <Clock3 size={13} /> : <CirclePlus size={16} />}</span></button> })}</div></article> })}</div>
    <div className="hidden overflow-x-auto md:block"><div className="min-w-[850px]"><div className="grid grid-cols-[100px_repeat(7,minmax(100px,1fr))] gap-2 border-b border-ink/10 pb-3 text-[11px] font-bold uppercase tracking-wider text-ink/45 dark:border-cream/10 dark:text-cream/45"><div>Momento</div>{weekdays.map((day, index) => { const date = new Date(weekStart); date.setDate(date.getDate() + index); return <div key={day} className={index === 0 ? 'text-coral' : ''}><div className="flex items-center gap-1">{formatDayName(date)}<span className="font-normal">{date.getDate()}</span><button onClick={() => onAddMeal({ day })} className="ml-auto rounded-full p-1 text-moss hover:bg-sage dark:hover:bg-moss/30" aria-label={`Agregar comida el ${day}`}><CirclePlus size={13} /></button></div></div> })}</div>{['Desayuno', 'Almuerzo', 'Merienda', 'Cena'].map((meal, mealIndex) => <div className="grid grid-cols-[100px_repeat(7,minmax(100px,1fr))] gap-2 border-b border-ink/10 py-3 last:border-0 dark:border-cream/10" key={meal}><div className="pt-3 text-xs font-bold text-ink/45 dark:text-cream/45">{meal}</div>{Object.entries(menu).map(([day, meals]) => { const mealName = getMealName(meals[meal]); return <button onClick={() => onAddMeal({ day, meal })} key={day} className={`meal-card group text-left ${mealName ? 'meal-card--filled' : ''}`}><span className={`mb-1.5 block text-sm font-semibold leading-snug ${mealName ? 'group-hover:text-coral' : 'text-moss dark:text-sage'}`}>{mealName || '+ Agregar comida'}</span><span className="flex items-center gap-1 text-[10px] text-ink/40 dark:text-cream/40">{mealName && <><Clock3 size={11} /> {mealIndex === 0 ? '10 min' : mealIndex === 1 ? '25 min' : '15 min'}</>}</span></button> })}</div>)}</div></div>
  </section>
}
